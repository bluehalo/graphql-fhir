/**
 * @name exports.getLinkage
 * @static
 * @summary Linkage resolver.
 */
module.exports.getLinkage = function getLinkage(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getLinkageList
 * @static
 * @summary Linkage list resolver.
 */
module.exports.getLinkageList = function getLinkageList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getLinkageInstance
 * @static
 * @summary Linkage instance resolver.
 */
module.exports.getLinkageInstance = function getLinkageInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createLinkage
 * @static
 * @summary Create Linkage resolver.
 */
module.exports.createLinkage = function createLinkage(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateLinkage
 * @static
 * @summary Update Linkage resolver.
 */
module.exports.updateLinkage = function updateLinkage(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeLinkage
 * @static
 * @summary Remove Linkage resolver.
 */
module.exports.removeLinkage = function removeLinkage(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
