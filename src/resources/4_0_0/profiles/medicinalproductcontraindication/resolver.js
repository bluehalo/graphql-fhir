/**
 * @name exports.getMedicinalProductContraindication
 * @static
 * @summary MedicinalProductContraindication resolver.
 */
module.exports.getMedicinalProductContraindication = function getMedicinalProductContraindication(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicinalProductContraindicationList
 * @static
 * @summary MedicinalProductContraindication list resolver.
 */
module.exports.getMedicinalProductContraindicationList = function getMedicinalProductContraindicationList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicinalProductContraindicationInstance
 * @static
 * @summary MedicinalProductContraindication instance resolver.
 */
module.exports.getMedicinalProductContraindicationInstance = function getMedicinalProductContraindicationInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createMedicinalProductContraindication
 * @static
 * @summary Create MedicinalProductContraindication resolver.
 */
module.exports.createMedicinalProductContraindication = function createMedicinalProductContraindication(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateMedicinalProductContraindication
 * @static
 * @summary Update MedicinalProductContraindication resolver.
 */
module.exports.updateMedicinalProductContraindication = function updateMedicinalProductContraindication(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeMedicinalProductContraindication
 * @static
 * @summary Remove MedicinalProductContraindication resolver.
 */
module.exports.removeMedicinalProductContraindication = function removeMedicinalProductContraindication(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
