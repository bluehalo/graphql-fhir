/**
 * @name exports.getChargeItem
 * @static
 * @summary ChargeItem resolver.
 */
module.exports.getChargeItem = function getChargeItem(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getChargeItemList
 * @static
 * @summary ChargeItem list resolver.
 */
module.exports.getChargeItemList = function getChargeItemList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getChargeItemInstance
 * @static
 * @summary ChargeItem instance resolver.
 */
module.exports.getChargeItemInstance = function getChargeItemInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createChargeItem
 * @static
 * @summary Create ChargeItem resolver.
 */
module.exports.createChargeItem = function createChargeItem(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateChargeItem
 * @static
 * @summary Update ChargeItem resolver.
 */
module.exports.updateChargeItem = function updateChargeItem(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeChargeItem
 * @static
 * @summary Remove ChargeItem resolver.
 */
module.exports.removeChargeItem = function removeChargeItem(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
