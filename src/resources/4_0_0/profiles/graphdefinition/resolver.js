/**
 * @name exports.getGraphDefinition
 * @static
 * @summary GraphDefinition resolver.
 */
module.exports.getGraphDefinition = function getGraphDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getGraphDefinitionList
 * @static
 * @summary GraphDefinition list resolver.
 */
module.exports.getGraphDefinitionList = function getGraphDefinitionList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getGraphDefinitionInstance
 * @static
 * @summary GraphDefinition instance resolver.
 */
module.exports.getGraphDefinitionInstance = function getGraphDefinitionInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createGraphDefinition
 * @static
 * @summary Create GraphDefinition resolver.
 */
module.exports.createGraphDefinition = function createGraphDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateGraphDefinition
 * @static
 * @summary Update GraphDefinition resolver.
 */
module.exports.updateGraphDefinition = function updateGraphDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeGraphDefinition
 * @static
 * @summary Remove GraphDefinition resolver.
 */
module.exports.removeGraphDefinition = function removeGraphDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
