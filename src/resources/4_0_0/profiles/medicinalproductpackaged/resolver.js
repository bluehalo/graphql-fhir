/**
 * @name exports.getMedicinalProductPackaged
 * @static
 * @summary MedicinalProductPackaged resolver.
 */
module.exports.getMedicinalProductPackaged = function getMedicinalProductPackaged(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicinalProductPackagedList
 * @static
 * @summary MedicinalProductPackaged list resolver.
 */
module.exports.getMedicinalProductPackagedList = function getMedicinalProductPackagedList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicinalProductPackagedInstance
 * @static
 * @summary MedicinalProductPackaged instance resolver.
 */
module.exports.getMedicinalProductPackagedInstance = function getMedicinalProductPackagedInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createMedicinalProductPackaged
 * @static
 * @summary Create MedicinalProductPackaged resolver.
 */
module.exports.createMedicinalProductPackaged = function createMedicinalProductPackaged(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateMedicinalProductPackaged
 * @static
 * @summary Update MedicinalProductPackaged resolver.
 */
module.exports.updateMedicinalProductPackaged = function updateMedicinalProductPackaged(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeMedicinalProductPackaged
 * @static
 * @summary Remove MedicinalProductPackaged resolver.
 */
module.exports.removeMedicinalProductPackaged = function removeMedicinalProductPackaged(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
