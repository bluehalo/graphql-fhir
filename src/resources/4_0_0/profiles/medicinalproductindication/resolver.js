/**
 * @name exports.getMedicinalProductIndication
 * @static
 * @summary MedicinalProductIndication resolver.
 */
module.exports.getMedicinalProductIndication = function getMedicinalProductIndication(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicinalProductIndicationList
 * @static
 * @summary MedicinalProductIndication list resolver.
 */
module.exports.getMedicinalProductIndicationList = function getMedicinalProductIndicationList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicinalProductIndicationInstance
 * @static
 * @summary MedicinalProductIndication instance resolver.
 */
module.exports.getMedicinalProductIndicationInstance = function getMedicinalProductIndicationInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createMedicinalProductIndication
 * @static
 * @summary Create MedicinalProductIndication resolver.
 */
module.exports.createMedicinalProductIndication = function createMedicinalProductIndication(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateMedicinalProductIndication
 * @static
 * @summary Update MedicinalProductIndication resolver.
 */
module.exports.updateMedicinalProductIndication = function updateMedicinalProductIndication(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeMedicinalProductIndication
 * @static
 * @summary Remove MedicinalProductIndication resolver.
 */
module.exports.removeMedicinalProductIndication = function removeMedicinalProductIndication(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
