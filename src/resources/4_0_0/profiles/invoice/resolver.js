/**
 * @name exports.getInvoice
 * @static
 * @summary Invoice resolver.
 */
module.exports.getInvoice = function getInvoice(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getInvoiceList
 * @static
 * @summary Invoice list resolver.
 */
module.exports.getInvoiceList = function getInvoiceList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getInvoiceInstance
 * @static
 * @summary Invoice instance resolver.
 */
module.exports.getInvoiceInstance = function getInvoiceInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createInvoice
 * @static
 * @summary Create Invoice resolver.
 */
module.exports.createInvoice = function createInvoice(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateInvoice
 * @static
 * @summary Update Invoice resolver.
 */
module.exports.updateInvoice = function updateInvoice(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeInvoice
 * @static
 * @summary Remove Invoice resolver.
 */
module.exports.removeInvoice = function removeInvoice(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
