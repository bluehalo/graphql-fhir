/**
 * @name exports.getMeasure
 * @static
 * @summary Measure resolver.
 */
module.exports.getMeasure = function getMeasure(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMeasureList
 * @static
 * @summary Measure list resolver.
 */
module.exports.getMeasureList = function getMeasureList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMeasureInstance
 * @static
 * @summary Measure instance resolver.
 */
module.exports.getMeasureInstance = function getMeasureInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createMeasure
 * @static
 * @summary Create Measure resolver.
 */
module.exports.createMeasure = function createMeasure(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateMeasure
 * @static
 * @summary Update Measure resolver.
 */
module.exports.updateMeasure = function updateMeasure(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeMeasure
 * @static
 * @summary Remove Measure resolver.
 */
module.exports.removeMeasure = function removeMeasure(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
