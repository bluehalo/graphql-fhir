/**
 * @name exports.getAdverseEvent
 * @static
 * @summary AdverseEvent resolver.
 */
module.exports.getAdverseEvent = function getAdverseEvent(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getAdverseEventList
 * @static
 * @summary AdverseEvent list resolver.
 */
module.exports.getAdverseEventList = function getAdverseEventList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getAdverseEventInstance
 * @static
 * @summary AdverseEvent instance resolver.
 */
module.exports.getAdverseEventInstance = function getAdverseEventInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createAdverseEvent
 * @static
 * @summary Create AdverseEvent resolver.
 */
module.exports.createAdverseEvent = function createAdverseEvent(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateAdverseEvent
 * @static
 * @summary Update AdverseEvent resolver.
 */
module.exports.updateAdverseEvent = function updateAdverseEvent(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeAdverseEvent
 * @static
 * @summary Remove AdverseEvent resolver.
 */
module.exports.removeAdverseEvent = function removeAdverseEvent(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
