/**
 * @name exports.getMedicinalProductAuthorization
 * @static
 * @summary MedicinalProductAuthorization resolver.
 */
module.exports.getMedicinalProductAuthorization = function getMedicinalProductAuthorization(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicinalProductAuthorizationList
 * @static
 * @summary MedicinalProductAuthorization list resolver.
 */
module.exports.getMedicinalProductAuthorizationList = function getMedicinalProductAuthorizationList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicinalProductAuthorizationInstance
 * @static
 * @summary MedicinalProductAuthorization instance resolver.
 */
module.exports.getMedicinalProductAuthorizationInstance = function getMedicinalProductAuthorizationInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createMedicinalProductAuthorization
 * @static
 * @summary Create MedicinalProductAuthorization resolver.
 */
module.exports.createMedicinalProductAuthorization = function createMedicinalProductAuthorization(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateMedicinalProductAuthorization
 * @static
 * @summary Update MedicinalProductAuthorization resolver.
 */
module.exports.updateMedicinalProductAuthorization = function updateMedicinalProductAuthorization(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeMedicinalProductAuthorization
 * @static
 * @summary Remove MedicinalProductAuthorization resolver.
 */
module.exports.removeMedicinalProductAuthorization = function removeMedicinalProductAuthorization(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
