/**
 * @name exports.getMedicationKnowledge
 * @static
 * @summary MedicationKnowledge resolver.
 */
module.exports.getMedicationKnowledge = function getMedicationKnowledge(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicationKnowledgeList
 * @static
 * @summary MedicationKnowledge list resolver.
 */
module.exports.getMedicationKnowledgeList = function getMedicationKnowledgeList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicationKnowledgeInstance
 * @static
 * @summary MedicationKnowledge instance resolver.
 */
module.exports.getMedicationKnowledgeInstance = function getMedicationKnowledgeInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createMedicationKnowledge
 * @static
 * @summary Create MedicationKnowledge resolver.
 */
module.exports.createMedicationKnowledge = function createMedicationKnowledge(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateMedicationKnowledge
 * @static
 * @summary Update MedicationKnowledge resolver.
 */
module.exports.updateMedicationKnowledge = function updateMedicationKnowledge(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeMedicationKnowledge
 * @static
 * @summary Remove MedicationKnowledge resolver.
 */
module.exports.removeMedicationKnowledge = function removeMedicationKnowledge(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
