/**
 * @name exports.getResearchStudy
 * @static
 * @summary ResearchStudy resolver.
 */
module.exports.getResearchStudy = function getResearchStudy(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getResearchStudyList
 * @static
 * @summary ResearchStudy list resolver.
 */
module.exports.getResearchStudyList = function getResearchStudyList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getResearchStudyInstance
 * @static
 * @summary ResearchStudy instance resolver.
 */
module.exports.getResearchStudyInstance = function getResearchStudyInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createResearchStudy
 * @static
 * @summary Create ResearchStudy resolver.
 */
module.exports.createResearchStudy = function createResearchStudy(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateResearchStudy
 * @static
 * @summary Update ResearchStudy resolver.
 */
module.exports.updateResearchStudy = function updateResearchStudy(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeResearchStudy
 * @static
 * @summary Remove ResearchStudy resolver.
 */
module.exports.removeResearchStudy = function removeResearchStudy(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
