/**
 * @name exports.getLibrary
 * @static
 * @summary Library resolver.
 */
module.exports.getLibrary = function getLibrary(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getLibraryList
 * @static
 * @summary Library list resolver.
 */
module.exports.getLibraryList = function getLibraryList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getLibraryInstance
 * @static
 * @summary Library instance resolver.
 */
module.exports.getLibraryInstance = function getLibraryInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createLibrary
 * @static
 * @summary Create Library resolver.
 */
module.exports.createLibrary = function createLibrary(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateLibrary
 * @static
 * @summary Update Library resolver.
 */
module.exports.updateLibrary = function updateLibrary(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeLibrary
 * @static
 * @summary Remove Library resolver.
 */
module.exports.removeLibrary = function removeLibrary(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
