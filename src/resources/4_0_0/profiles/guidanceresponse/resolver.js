/**
 * @name exports.getGuidanceResponse
 * @static
 * @summary GuidanceResponse resolver.
 */
module.exports.getGuidanceResponse = function getGuidanceResponse(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getGuidanceResponseList
 * @static
 * @summary GuidanceResponse list resolver.
 */
module.exports.getGuidanceResponseList = function getGuidanceResponseList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getGuidanceResponseInstance
 * @static
 * @summary GuidanceResponse instance resolver.
 */
module.exports.getGuidanceResponseInstance = function getGuidanceResponseInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createGuidanceResponse
 * @static
 * @summary Create GuidanceResponse resolver.
 */
module.exports.createGuidanceResponse = function createGuidanceResponse(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateGuidanceResponse
 * @static
 * @summary Update GuidanceResponse resolver.
 */
module.exports.updateGuidanceResponse = function updateGuidanceResponse(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeGuidanceResponse
 * @static
 * @summary Remove GuidanceResponse resolver.
 */
module.exports.removeGuidanceResponse = function removeGuidanceResponse(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
