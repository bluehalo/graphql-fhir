/**
 * @name exports.getMedicinalProductInteraction
 * @static
 * @summary MedicinalProductInteraction resolver.
 */
module.exports.getMedicinalProductInteraction = function getMedicinalProductInteraction(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicinalProductInteractionList
 * @static
 * @summary MedicinalProductInteraction list resolver.
 */
module.exports.getMedicinalProductInteractionList = function getMedicinalProductInteractionList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicinalProductInteractionInstance
 * @static
 * @summary MedicinalProductInteraction instance resolver.
 */
module.exports.getMedicinalProductInteractionInstance = function getMedicinalProductInteractionInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createMedicinalProductInteraction
 * @static
 * @summary Create MedicinalProductInteraction resolver.
 */
module.exports.createMedicinalProductInteraction = function createMedicinalProductInteraction(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateMedicinalProductInteraction
 * @static
 * @summary Update MedicinalProductInteraction resolver.
 */
module.exports.updateMedicinalProductInteraction = function updateMedicinalProductInteraction(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeMedicinalProductInteraction
 * @static
 * @summary Remove MedicinalProductInteraction resolver.
 */
module.exports.removeMedicinalProductInteraction = function removeMedicinalProductInteraction(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
