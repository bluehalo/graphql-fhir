/**
 * @name exports.getMessageDefinition
 * @static
 * @summary MessageDefinition resolver.
 */
module.exports.getMessageDefinition = function getMessageDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMessageDefinitionList
 * @static
 * @summary MessageDefinition list resolver.
 */
module.exports.getMessageDefinitionList = function getMessageDefinitionList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMessageDefinitionInstance
 * @static
 * @summary MessageDefinition instance resolver.
 */
module.exports.getMessageDefinitionInstance = function getMessageDefinitionInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createMessageDefinition
 * @static
 * @summary Create MessageDefinition resolver.
 */
module.exports.createMessageDefinition = function createMessageDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateMessageDefinition
 * @static
 * @summary Update MessageDefinition resolver.
 */
module.exports.updateMessageDefinition = function updateMessageDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeMessageDefinition
 * @static
 * @summary Remove MessageDefinition resolver.
 */
module.exports.removeMessageDefinition = function removeMessageDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
