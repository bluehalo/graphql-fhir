/**
 * @name exports.getConsent
 * @static
 * @summary Consent resolver.
 */
module.exports.getConsent = function getConsent(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getConsentList
 * @static
 * @summary Consent list resolver.
 */
module.exports.getConsentList = function getConsentList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getConsentInstance
 * @static
 * @summary Consent instance resolver.
 */
module.exports.getConsentInstance = function getConsentInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createConsent
 * @static
 * @summary Create Consent resolver.
 */
module.exports.createConsent = function createConsent(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateConsent
 * @static
 * @summary Update Consent resolver.
 */
module.exports.updateConsent = function updateConsent(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeConsent
 * @static
 * @summary Remove Consent resolver.
 */
module.exports.removeConsent = function removeConsent(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
