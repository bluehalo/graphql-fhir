/**
 * @name exports.getStructureMap
 * @static
 * @summary StructureMap resolver.
 */
module.exports.getStructureMap = function getStructureMap(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getStructureMapList
 * @static
 * @summary StructureMap list resolver.
 */
module.exports.getStructureMapList = function getStructureMapList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getStructureMapInstance
 * @static
 * @summary StructureMap instance resolver.
 */
module.exports.getStructureMapInstance = function getStructureMapInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createStructureMap
 * @static
 * @summary Create StructureMap resolver.
 */
module.exports.createStructureMap = function createStructureMap(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateStructureMap
 * @static
 * @summary Update StructureMap resolver.
 */
module.exports.updateStructureMap = function updateStructureMap(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeStructureMap
 * @static
 * @summary Remove StructureMap resolver.
 */
module.exports.removeStructureMap = function removeStructureMap(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
