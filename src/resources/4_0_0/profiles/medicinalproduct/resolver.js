/**
 * @name exports.getMedicinalProduct
 * @static
 * @summary MedicinalProduct resolver.
 */
module.exports.getMedicinalProduct = function getMedicinalProduct(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicinalProductList
 * @static
 * @summary MedicinalProduct list resolver.
 */
module.exports.getMedicinalProductList = function getMedicinalProductList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicinalProductInstance
 * @static
 * @summary MedicinalProduct instance resolver.
 */
module.exports.getMedicinalProductInstance = function getMedicinalProductInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createMedicinalProduct
 * @static
 * @summary Create MedicinalProduct resolver.
 */
module.exports.createMedicinalProduct = function createMedicinalProduct(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateMedicinalProduct
 * @static
 * @summary Update MedicinalProduct resolver.
 */
module.exports.updateMedicinalProduct = function updateMedicinalProduct(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeMedicinalProduct
 * @static
 * @summary Remove MedicinalProduct resolver.
 */
module.exports.removeMedicinalProduct = function removeMedicinalProduct(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
