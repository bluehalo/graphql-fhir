/**
 * @name exports.getChargeItemDefinition
 * @static
 * @summary ChargeItemDefinition resolver.
 */
module.exports.getChargeItemDefinition = function getChargeItemDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getChargeItemDefinitionList
 * @static
 * @summary ChargeItemDefinition list resolver.
 */
module.exports.getChargeItemDefinitionList = function getChargeItemDefinitionList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getChargeItemDefinitionInstance
 * @static
 * @summary ChargeItemDefinition instance resolver.
 */
module.exports.getChargeItemDefinitionInstance = function getChargeItemDefinitionInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createChargeItemDefinition
 * @static
 * @summary Create ChargeItemDefinition resolver.
 */
module.exports.createChargeItemDefinition = function createChargeItemDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateChargeItemDefinition
 * @static
 * @summary Update ChargeItemDefinition resolver.
 */
module.exports.updateChargeItemDefinition = function updateChargeItemDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeChargeItemDefinition
 * @static
 * @summary Remove ChargeItemDefinition resolver.
 */
module.exports.removeChargeItemDefinition = function removeChargeItemDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
