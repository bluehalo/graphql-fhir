/**
 * @name exports.getTask
 * @static
 * @summary Task resolver.
 */
module.exports.getTask = function getTask(root, args, context = {}, info) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getTaskList
 * @static
 * @summary Task list resolver.
 */
module.exports.getTaskList = function getTaskList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getTaskInstance
 * @static
 * @summary Task instance resolver.
 */
module.exports.getTaskInstance = function getTaskInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createTask
 * @static
 * @summary Create Task resolver.
 */
module.exports.createTask = function createTask(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateTask
 * @static
 * @summary Update Task resolver.
 */
module.exports.updateTask = function updateTask(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeTask
 * @static
 * @summary Remove Task resolver.
 */
module.exports.removeTask = function removeTask(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
