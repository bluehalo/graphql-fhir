/**
 * @name exports.getActivityDefinition
 * @static
 * @summary ActivityDefinition resolver.
 */
module.exports.getActivityDefinition = function getActivityDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getActivityDefinitionList
 * @static
 * @summary ActivityDefinition list resolver.
 */
module.exports.getActivityDefinitionList = function getActivityDefinitionList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getActivityDefinitionInstance
 * @static
 * @summary ActivityDefinition instance resolver.
 */
module.exports.getActivityDefinitionInstance = function getActivityDefinitionInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createActivityDefinition
 * @static
 * @summary Create ActivityDefinition resolver.
 */
module.exports.createActivityDefinition = function createActivityDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateActivityDefinition
 * @static
 * @summary Update ActivityDefinition resolver.
 */
module.exports.updateActivityDefinition = function updateActivityDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeActivityDefinition
 * @static
 * @summary Remove ActivityDefinition resolver.
 */
module.exports.removeActivityDefinition = function removeActivityDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
