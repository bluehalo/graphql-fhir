/**
 * @name exports.getDeviceRequest
 * @static
 * @summary DeviceRequest resolver.
 */
module.exports.getDeviceRequest = function getDeviceRequest(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getDeviceRequestList
 * @static
 * @summary DeviceRequest list resolver.
 */
module.exports.getDeviceRequestList = function getDeviceRequestList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getDeviceRequestInstance
 * @static
 * @summary DeviceRequest instance resolver.
 */
module.exports.getDeviceRequestInstance = function getDeviceRequestInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createDeviceRequest
 * @static
 * @summary Create DeviceRequest resolver.
 */
module.exports.createDeviceRequest = function createDeviceRequest(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateDeviceRequest
 * @static
 * @summary Update DeviceRequest resolver.
 */
module.exports.updateDeviceRequest = function updateDeviceRequest(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeDeviceRequest
 * @static
 * @summary Remove DeviceRequest resolver.
 */
module.exports.removeDeviceRequest = function removeDeviceRequest(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
