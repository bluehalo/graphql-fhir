/**
 * @name exports.getMedicinalProductUndesirableEffect
 * @static
 * @summary MedicinalProductUndesirableEffect resolver.
 */
module.exports.getMedicinalProductUndesirableEffect = function getMedicinalProductUndesirableEffect(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicinalProductUndesirableEffectList
 * @static
 * @summary MedicinalProductUndesirableEffect list resolver.
 */
module.exports.getMedicinalProductUndesirableEffectList = function getMedicinalProductUndesirableEffectList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicinalProductUndesirableEffectInstance
 * @static
 * @summary MedicinalProductUndesirableEffect instance resolver.
 */
module.exports.getMedicinalProductUndesirableEffectInstance = function getMedicinalProductUndesirableEffectInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createMedicinalProductUndesirableEffect
 * @static
 * @summary Create MedicinalProductUndesirableEffect resolver.
 */
module.exports.createMedicinalProductUndesirableEffect = function createMedicinalProductUndesirableEffect(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateMedicinalProductUndesirableEffect
 * @static
 * @summary Update MedicinalProductUndesirableEffect resolver.
 */
module.exports.updateMedicinalProductUndesirableEffect = function updateMedicinalProductUndesirableEffect(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeMedicinalProductUndesirableEffect
 * @static
 * @summary Remove MedicinalProductUndesirableEffect resolver.
 */
module.exports.removeMedicinalProductUndesirableEffect = function removeMedicinalProductUndesirableEffect(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
