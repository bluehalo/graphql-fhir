/**
 * @name exports.getServiceRequest
 * @static
 * @summary ServiceRequest resolver.
 */
module.exports.getServiceRequest = function getServiceRequest(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getServiceRequestList
 * @static
 * @summary ServiceRequest list resolver.
 */
module.exports.getServiceRequestList = function getServiceRequestList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getServiceRequestInstance
 * @static
 * @summary ServiceRequest instance resolver.
 */
module.exports.getServiceRequestInstance = function getServiceRequestInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createServiceRequest
 * @static
 * @summary Create ServiceRequest resolver.
 */
module.exports.createServiceRequest = function createServiceRequest(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateServiceRequest
 * @static
 * @summary Update ServiceRequest resolver.
 */
module.exports.updateServiceRequest = function updateServiceRequest(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeServiceRequest
 * @static
 * @summary Remove ServiceRequest resolver.
 */
module.exports.removeServiceRequest = function removeServiceRequest(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
