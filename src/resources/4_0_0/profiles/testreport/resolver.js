/**
 * @name exports.getTestReport
 * @static
 * @summary TestReport resolver.
 */
module.exports.getTestReport = function getTestReport(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getTestReportList
 * @static
 * @summary TestReport list resolver.
 */
module.exports.getTestReportList = function getTestReportList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getTestReportInstance
 * @static
 * @summary TestReport instance resolver.
 */
module.exports.getTestReportInstance = function getTestReportInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createTestReport
 * @static
 * @summary Create TestReport resolver.
 */
module.exports.createTestReport = function createTestReport(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateTestReport
 * @static
 * @summary Update TestReport resolver.
 */
module.exports.updateTestReport = function updateTestReport(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeTestReport
 * @static
 * @summary Remove TestReport resolver.
 */
module.exports.removeTestReport = function removeTestReport(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
