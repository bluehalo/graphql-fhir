/**
 * @name exports.getCompartmentDefinition
 * @static
 * @summary CompartmentDefinition resolver.
 */
module.exports.getCompartmentDefinition = function getCompartmentDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getCompartmentDefinitionList
 * @static
 * @summary CompartmentDefinition list resolver.
 */
module.exports.getCompartmentDefinitionList = function getCompartmentDefinitionList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getCompartmentDefinitionInstance
 * @static
 * @summary CompartmentDefinition instance resolver.
 */
module.exports.getCompartmentDefinitionInstance = function getCompartmentDefinitionInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createCompartmentDefinition
 * @static
 * @summary Create CompartmentDefinition resolver.
 */
module.exports.createCompartmentDefinition = function createCompartmentDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateCompartmentDefinition
 * @static
 * @summary Update CompartmentDefinition resolver.
 */
module.exports.updateCompartmentDefinition = function updateCompartmentDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeCompartmentDefinition
 * @static
 * @summary Remove CompartmentDefinition resolver.
 */
module.exports.removeCompartmentDefinition = function removeCompartmentDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
