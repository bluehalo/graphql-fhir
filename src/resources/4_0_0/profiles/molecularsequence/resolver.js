/**
 * @name exports.getMolecularSequence
 * @static
 * @summary MolecularSequence resolver.
 */
module.exports.getMolecularSequence = function getMolecularSequence(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMolecularSequenceList
 * @static
 * @summary MolecularSequence list resolver.
 */
module.exports.getMolecularSequenceList = function getMolecularSequenceList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMolecularSequenceInstance
 * @static
 * @summary MolecularSequence instance resolver.
 */
module.exports.getMolecularSequenceInstance = function getMolecularSequenceInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createMolecularSequence
 * @static
 * @summary Create MolecularSequence resolver.
 */
module.exports.createMolecularSequence = function createMolecularSequence(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateMolecularSequence
 * @static
 * @summary Update MolecularSequence resolver.
 */
module.exports.updateMolecularSequence = function updateMolecularSequence(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeMolecularSequence
 * @static
 * @summary Remove MolecularSequence resolver.
 */
module.exports.removeMolecularSequence = function removeMolecularSequence(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
