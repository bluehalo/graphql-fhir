/**
 * @name exports.getBodyStructure
 * @static
 * @summary BodyStructure resolver.
 */
module.exports.getBodyStructure = function getBodyStructure(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getBodyStructureList
 * @static
 * @summary BodyStructure list resolver.
 */
module.exports.getBodyStructureList = function getBodyStructureList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getBodyStructureInstance
 * @static
 * @summary BodyStructure instance resolver.
 */
module.exports.getBodyStructureInstance = function getBodyStructureInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createBodyStructure
 * @static
 * @summary Create BodyStructure resolver.
 */
module.exports.createBodyStructure = function createBodyStructure(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateBodyStructure
 * @static
 * @summary Update BodyStructure resolver.
 */
module.exports.updateBodyStructure = function updateBodyStructure(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeBodyStructure
 * @static
 * @summary Remove BodyStructure resolver.
 */
module.exports.removeBodyStructure = function removeBodyStructure(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
