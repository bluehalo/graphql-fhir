/**
 * @name exports.getSubstanceSpecification
 * @static
 * @summary SubstanceSpecification resolver.
 */
module.exports.getSubstanceSpecification = function getSubstanceSpecification(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getSubstanceSpecificationList
 * @static
 * @summary SubstanceSpecification list resolver.
 */
module.exports.getSubstanceSpecificationList = function getSubstanceSpecificationList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getSubstanceSpecificationInstance
 * @static
 * @summary SubstanceSpecification instance resolver.
 */
module.exports.getSubstanceSpecificationInstance = function getSubstanceSpecificationInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createSubstanceSpecification
 * @static
 * @summary Create SubstanceSpecification resolver.
 */
module.exports.createSubstanceSpecification = function createSubstanceSpecification(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateSubstanceSpecification
 * @static
 * @summary Update SubstanceSpecification resolver.
 */
module.exports.updateSubstanceSpecification = function updateSubstanceSpecification(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeSubstanceSpecification
 * @static
 * @summary Remove SubstanceSpecification resolver.
 */
module.exports.removeSubstanceSpecification = function removeSubstanceSpecification(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
