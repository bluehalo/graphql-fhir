/**
 * @name exports.getVerificationResult
 * @static
 * @summary VerificationResult resolver.
 */
module.exports.getVerificationResult = function getVerificationResult(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getVerificationResultList
 * @static
 * @summary VerificationResult list resolver.
 */
module.exports.getVerificationResultList = function getVerificationResultList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getVerificationResultInstance
 * @static
 * @summary VerificationResult instance resolver.
 */
module.exports.getVerificationResultInstance = function getVerificationResultInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createVerificationResult
 * @static
 * @summary Create VerificationResult resolver.
 */
module.exports.createVerificationResult = function createVerificationResult(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateVerificationResult
 * @static
 * @summary Update VerificationResult resolver.
 */
module.exports.updateVerificationResult = function updateVerificationResult(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeVerificationResult
 * @static
 * @summary Remove VerificationResult resolver.
 */
module.exports.removeVerificationResult = function removeVerificationResult(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
