/**
 * @name exports.getCapabilityStatement
 * @static
 * @summary CapabilityStatement resolver.
 */
module.exports.getCapabilityStatement = function getCapabilityStatement(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getCapabilityStatementList
 * @static
 * @summary CapabilityStatement list resolver.
 */
module.exports.getCapabilityStatementList = function getCapabilityStatementList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getCapabilityStatementInstance
 * @static
 * @summary CapabilityStatement instance resolver.
 */
module.exports.getCapabilityStatementInstance = function getCapabilityStatementInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createCapabilityStatement
 * @static
 * @summary Create CapabilityStatement resolver.
 */
module.exports.createCapabilityStatement = function createCapabilityStatement(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateCapabilityStatement
 * @static
 * @summary Update CapabilityStatement resolver.
 */
module.exports.updateCapabilityStatement = function updateCapabilityStatement(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeCapabilityStatement
 * @static
 * @summary Remove CapabilityStatement resolver.
 */
module.exports.removeCapabilityStatement = function removeCapabilityStatement(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
