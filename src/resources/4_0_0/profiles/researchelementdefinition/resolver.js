/**
 * @name exports.getResearchElementDefinition
 * @static
 * @summary ResearchElementDefinition resolver.
 */
module.exports.getResearchElementDefinition = function getResearchElementDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getResearchElementDefinitionList
 * @static
 * @summary ResearchElementDefinition list resolver.
 */
module.exports.getResearchElementDefinitionList = function getResearchElementDefinitionList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getResearchElementDefinitionInstance
 * @static
 * @summary ResearchElementDefinition instance resolver.
 */
module.exports.getResearchElementDefinitionInstance = function getResearchElementDefinitionInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createResearchElementDefinition
 * @static
 * @summary Create ResearchElementDefinition resolver.
 */
module.exports.createResearchElementDefinition = function createResearchElementDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateResearchElementDefinition
 * @static
 * @summary Update ResearchElementDefinition resolver.
 */
module.exports.updateResearchElementDefinition = function updateResearchElementDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeResearchElementDefinition
 * @static
 * @summary Remove ResearchElementDefinition resolver.
 */
module.exports.removeResearchElementDefinition = function removeResearchElementDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
