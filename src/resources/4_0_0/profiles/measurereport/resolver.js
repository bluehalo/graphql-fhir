/**
 * @name exports.getMeasureReport
 * @static
 * @summary MeasureReport resolver.
 */
module.exports.getMeasureReport = function getMeasureReport(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMeasureReportList
 * @static
 * @summary MeasureReport list resolver.
 */
module.exports.getMeasureReportList = function getMeasureReportList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMeasureReportInstance
 * @static
 * @summary MeasureReport instance resolver.
 */
module.exports.getMeasureReportInstance = function getMeasureReportInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createMeasureReport
 * @static
 * @summary Create MeasureReport resolver.
 */
module.exports.createMeasureReport = function createMeasureReport(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateMeasureReport
 * @static
 * @summary Update MeasureReport resolver.
 */
module.exports.updateMeasureReport = function updateMeasureReport(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeMeasureReport
 * @static
 * @summary Remove MeasureReport resolver.
 */
module.exports.removeMeasureReport = function removeMeasureReport(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
