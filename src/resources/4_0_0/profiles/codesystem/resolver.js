/**
 * @name exports.getCodeSystem
 * @static
 * @summary CodeSystem resolver.
 */
module.exports.getCodeSystem = function getCodeSystem(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getCodeSystemList
 * @static
 * @summary CodeSystem list resolver.
 */
module.exports.getCodeSystemList = function getCodeSystemList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getCodeSystemInstance
 * @static
 * @summary CodeSystem instance resolver.
 */
module.exports.getCodeSystemInstance = function getCodeSystemInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createCodeSystem
 * @static
 * @summary Create CodeSystem resolver.
 */
module.exports.createCodeSystem = function createCodeSystem(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateCodeSystem
 * @static
 * @summary Update CodeSystem resolver.
 */
module.exports.updateCodeSystem = function updateCodeSystem(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeCodeSystem
 * @static
 * @summary Remove CodeSystem resolver.
 */
module.exports.removeCodeSystem = function removeCodeSystem(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
