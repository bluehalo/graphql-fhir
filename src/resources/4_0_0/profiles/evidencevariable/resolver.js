/**
 * @name exports.getEvidenceVariable
 * @static
 * @summary EvidenceVariable resolver.
 */
module.exports.getEvidenceVariable = function getEvidenceVariable(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getEvidenceVariableList
 * @static
 * @summary EvidenceVariable list resolver.
 */
module.exports.getEvidenceVariableList = function getEvidenceVariableList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getEvidenceVariableInstance
 * @static
 * @summary EvidenceVariable instance resolver.
 */
module.exports.getEvidenceVariableInstance = function getEvidenceVariableInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createEvidenceVariable
 * @static
 * @summary Create EvidenceVariable resolver.
 */
module.exports.createEvidenceVariable = function createEvidenceVariable(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateEvidenceVariable
 * @static
 * @summary Update EvidenceVariable resolver.
 */
module.exports.updateEvidenceVariable = function updateEvidenceVariable(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeEvidenceVariable
 * @static
 * @summary Remove EvidenceVariable resolver.
 */
module.exports.removeEvidenceVariable = function removeEvidenceVariable(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
