/**
 * @name exports.getCoverageEligibilityResponse
 * @static
 * @summary CoverageEligibilityResponse resolver.
 */
module.exports.getCoverageEligibilityResponse = function getCoverageEligibilityResponse(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getCoverageEligibilityResponseList
 * @static
 * @summary CoverageEligibilityResponse list resolver.
 */
module.exports.getCoverageEligibilityResponseList = function getCoverageEligibilityResponseList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getCoverageEligibilityResponseInstance
 * @static
 * @summary CoverageEligibilityResponse instance resolver.
 */
module.exports.getCoverageEligibilityResponseInstance = function getCoverageEligibilityResponseInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createCoverageEligibilityResponse
 * @static
 * @summary Create CoverageEligibilityResponse resolver.
 */
module.exports.createCoverageEligibilityResponse = function createCoverageEligibilityResponse(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateCoverageEligibilityResponse
 * @static
 * @summary Update CoverageEligibilityResponse resolver.
 */
module.exports.updateCoverageEligibilityResponse = function updateCoverageEligibilityResponse(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeCoverageEligibilityResponse
 * @static
 * @summary Remove CoverageEligibilityResponse resolver.
 */
module.exports.removeCoverageEligibilityResponse = function removeCoverageEligibilityResponse(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
