/**
 * @name exports.getSpecimenDefinition
 * @static
 * @summary SpecimenDefinition resolver.
 */
module.exports.getSpecimenDefinition = function getSpecimenDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getSpecimenDefinitionList
 * @static
 * @summary SpecimenDefinition list resolver.
 */
module.exports.getSpecimenDefinitionList = function getSpecimenDefinitionList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getSpecimenDefinitionInstance
 * @static
 * @summary SpecimenDefinition instance resolver.
 */
module.exports.getSpecimenDefinitionInstance = function getSpecimenDefinitionInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createSpecimenDefinition
 * @static
 * @summary Create SpecimenDefinition resolver.
 */
module.exports.createSpecimenDefinition = function createSpecimenDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateSpecimenDefinition
 * @static
 * @summary Update SpecimenDefinition resolver.
 */
module.exports.updateSpecimenDefinition = function updateSpecimenDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeSpecimenDefinition
 * @static
 * @summary Remove SpecimenDefinition resolver.
 */
module.exports.removeSpecimenDefinition = function removeSpecimenDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
