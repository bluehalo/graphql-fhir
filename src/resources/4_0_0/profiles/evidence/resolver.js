/**
 * @name exports.getEvidence
 * @static
 * @summary Evidence resolver.
 */
module.exports.getEvidence = function getEvidence(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getEvidenceList
 * @static
 * @summary Evidence list resolver.
 */
module.exports.getEvidenceList = function getEvidenceList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getEvidenceInstance
 * @static
 * @summary Evidence instance resolver.
 */
module.exports.getEvidenceInstance = function getEvidenceInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createEvidence
 * @static
 * @summary Create Evidence resolver.
 */
module.exports.createEvidence = function createEvidence(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateEvidence
 * @static
 * @summary Update Evidence resolver.
 */
module.exports.updateEvidence = function updateEvidence(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeEvidence
 * @static
 * @summary Remove Evidence resolver.
 */
module.exports.removeEvidence = function removeEvidence(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
