/**
 * @name exports.getMedicationRequest
 * @static
 * @summary MedicationRequest resolver.
 */
module.exports.getMedicationRequest = function getMedicationRequest(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicationRequestList
 * @static
 * @summary MedicationRequest list resolver.
 */
module.exports.getMedicationRequestList = function getMedicationRequestList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicationRequestInstance
 * @static
 * @summary MedicationRequest instance resolver.
 */
module.exports.getMedicationRequestInstance = function getMedicationRequestInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createMedicationRequest
 * @static
 * @summary Create MedicationRequest resolver.
 */
module.exports.createMedicationRequest = function createMedicationRequest(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateMedicationRequest
 * @static
 * @summary Update MedicationRequest resolver.
 */
module.exports.updateMedicationRequest = function updateMedicationRequest(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeMedicationRequest
 * @static
 * @summary Remove MedicationRequest resolver.
 */
module.exports.removeMedicationRequest = function removeMedicationRequest(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
