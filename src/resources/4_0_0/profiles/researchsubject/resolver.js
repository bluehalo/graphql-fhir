/**
 * @name exports.getResearchSubject
 * @static
 * @summary ResearchSubject resolver.
 */
module.exports.getResearchSubject = function getResearchSubject(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getResearchSubjectList
 * @static
 * @summary ResearchSubject list resolver.
 */
module.exports.getResearchSubjectList = function getResearchSubjectList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getResearchSubjectInstance
 * @static
 * @summary ResearchSubject instance resolver.
 */
module.exports.getResearchSubjectInstance = function getResearchSubjectInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createResearchSubject
 * @static
 * @summary Create ResearchSubject resolver.
 */
module.exports.createResearchSubject = function createResearchSubject(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateResearchSubject
 * @static
 * @summary Update ResearchSubject resolver.
 */
module.exports.updateResearchSubject = function updateResearchSubject(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeResearchSubject
 * @static
 * @summary Remove ResearchSubject resolver.
 */
module.exports.removeResearchSubject = function removeResearchSubject(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
