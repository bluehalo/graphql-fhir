/**
 * @name exports.getPlanDefinition
 * @static
 * @summary PlanDefinition resolver.
 */
module.exports.getPlanDefinition = function getPlanDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getPlanDefinitionList
 * @static
 * @summary PlanDefinition list resolver.
 */
module.exports.getPlanDefinitionList = function getPlanDefinitionList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getPlanDefinitionInstance
 * @static
 * @summary PlanDefinition instance resolver.
 */
module.exports.getPlanDefinitionInstance = function getPlanDefinitionInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createPlanDefinition
 * @static
 * @summary Create PlanDefinition resolver.
 */
module.exports.createPlanDefinition = function createPlanDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updatePlanDefinition
 * @static
 * @summary Update PlanDefinition resolver.
 */
module.exports.updatePlanDefinition = function updatePlanDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removePlanDefinition
 * @static
 * @summary Remove PlanDefinition resolver.
 */
module.exports.removePlanDefinition = function removePlanDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
