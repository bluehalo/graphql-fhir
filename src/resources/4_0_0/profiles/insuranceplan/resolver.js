/**
 * @name exports.getInsurancePlan
 * @static
 * @summary InsurancePlan resolver.
 */
module.exports.getInsurancePlan = function getInsurancePlan(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getInsurancePlanList
 * @static
 * @summary InsurancePlan list resolver.
 */
module.exports.getInsurancePlanList = function getInsurancePlanList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getInsurancePlanInstance
 * @static
 * @summary InsurancePlan instance resolver.
 */
module.exports.getInsurancePlanInstance = function getInsurancePlanInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createInsurancePlan
 * @static
 * @summary Create InsurancePlan resolver.
 */
module.exports.createInsurancePlan = function createInsurancePlan(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateInsurancePlan
 * @static
 * @summary Update InsurancePlan resolver.
 */
module.exports.updateInsurancePlan = function updateInsurancePlan(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeInsurancePlan
 * @static
 * @summary Remove InsurancePlan resolver.
 */
module.exports.removeInsurancePlan = function removeInsurancePlan(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
