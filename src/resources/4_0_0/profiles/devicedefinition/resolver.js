/**
 * @name exports.getDeviceDefinition
 * @static
 * @summary DeviceDefinition resolver.
 */
module.exports.getDeviceDefinition = function getDeviceDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getDeviceDefinitionList
 * @static
 * @summary DeviceDefinition list resolver.
 */
module.exports.getDeviceDefinitionList = function getDeviceDefinitionList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getDeviceDefinitionInstance
 * @static
 * @summary DeviceDefinition instance resolver.
 */
module.exports.getDeviceDefinitionInstance = function getDeviceDefinitionInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createDeviceDefinition
 * @static
 * @summary Create DeviceDefinition resolver.
 */
module.exports.createDeviceDefinition = function createDeviceDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateDeviceDefinition
 * @static
 * @summary Update DeviceDefinition resolver.
 */
module.exports.updateDeviceDefinition = function updateDeviceDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeDeviceDefinition
 * @static
 * @summary Remove DeviceDefinition resolver.
 */
module.exports.removeDeviceDefinition = function removeDeviceDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
