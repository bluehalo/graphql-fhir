/**
 * @name exports.getRequestGroup
 * @static
 * @summary RequestGroup resolver.
 */
module.exports.getRequestGroup = function getRequestGroup(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getRequestGroupList
 * @static
 * @summary RequestGroup list resolver.
 */
module.exports.getRequestGroupList = function getRequestGroupList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getRequestGroupInstance
 * @static
 * @summary RequestGroup instance resolver.
 */
module.exports.getRequestGroupInstance = function getRequestGroupInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createRequestGroup
 * @static
 * @summary Create RequestGroup resolver.
 */
module.exports.createRequestGroup = function createRequestGroup(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateRequestGroup
 * @static
 * @summary Update RequestGroup resolver.
 */
module.exports.updateRequestGroup = function updateRequestGroup(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeRequestGroup
 * @static
 * @summary Remove RequestGroup resolver.
 */
module.exports.removeRequestGroup = function removeRequestGroup(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
