/**
 * @name exports.getMedicinalProductPharmaceutical
 * @static
 * @summary MedicinalProductPharmaceutical resolver.
 */
module.exports.getMedicinalProductPharmaceutical = function getMedicinalProductPharmaceutical(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicinalProductPharmaceuticalList
 * @static
 * @summary MedicinalProductPharmaceutical list resolver.
 */
module.exports.getMedicinalProductPharmaceuticalList = function getMedicinalProductPharmaceuticalList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getMedicinalProductPharmaceuticalInstance
 * @static
 * @summary MedicinalProductPharmaceutical instance resolver.
 */
module.exports.getMedicinalProductPharmaceuticalInstance = function getMedicinalProductPharmaceuticalInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createMedicinalProductPharmaceutical
 * @static
 * @summary Create MedicinalProductPharmaceutical resolver.
 */
module.exports.createMedicinalProductPharmaceutical = function createMedicinalProductPharmaceutical(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateMedicinalProductPharmaceutical
 * @static
 * @summary Update MedicinalProductPharmaceutical resolver.
 */
module.exports.updateMedicinalProductPharmaceutical = function updateMedicinalProductPharmaceutical(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeMedicinalProductPharmaceutical
 * @static
 * @summary Remove MedicinalProductPharmaceutical resolver.
 */
module.exports.removeMedicinalProductPharmaceutical = function removeMedicinalProductPharmaceutical(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
