/**
 * @name exports.getEventDefinition
 * @static
 * @summary EventDefinition resolver.
 */
module.exports.getEventDefinition = function getEventDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getEventDefinitionList
 * @static
 * @summary EventDefinition list resolver.
 */
module.exports.getEventDefinitionList = function getEventDefinitionList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getEventDefinitionInstance
 * @static
 * @summary EventDefinition instance resolver.
 */
module.exports.getEventDefinitionInstance = function getEventDefinitionInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createEventDefinition
 * @static
 * @summary Create EventDefinition resolver.
 */
module.exports.createEventDefinition = function createEventDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateEventDefinition
 * @static
 * @summary Update EventDefinition resolver.
 */
module.exports.updateEventDefinition = function updateEventDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeEventDefinition
 * @static
 * @summary Remove EventDefinition resolver.
 */
module.exports.removeEventDefinition = function removeEventDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
