/**
 * @name exports.getEndpoint
 * @static
 * @summary Endpoint resolver.
 */
module.exports.getEndpoint = function getEndpoint(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getEndpointList
 * @static
 * @summary Endpoint list resolver.
 */
module.exports.getEndpointList = function getEndpointList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getEndpointInstance
 * @static
 * @summary Endpoint instance resolver.
 */
module.exports.getEndpointInstance = function getEndpointInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createEndpoint
 * @static
 * @summary Create Endpoint resolver.
 */
module.exports.createEndpoint = function createEndpoint(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateEndpoint
 * @static
 * @summary Update Endpoint resolver.
 */
module.exports.updateEndpoint = function updateEndpoint(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeEndpoint
 * @static
 * @summary Remove Endpoint resolver.
 */
module.exports.removeEndpoint = function removeEndpoint(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
