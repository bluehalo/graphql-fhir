/**
 * @name exports.getResearchDefinition
 * @static
 * @summary ResearchDefinition resolver.
 */
module.exports.getResearchDefinition = function getResearchDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getResearchDefinitionList
 * @static
 * @summary ResearchDefinition list resolver.
 */
module.exports.getResearchDefinitionList = function getResearchDefinitionList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getResearchDefinitionInstance
 * @static
 * @summary ResearchDefinition instance resolver.
 */
module.exports.getResearchDefinitionInstance = function getResearchDefinitionInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createResearchDefinition
 * @static
 * @summary Create ResearchDefinition resolver.
 */
module.exports.createResearchDefinition = function createResearchDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateResearchDefinition
 * @static
 * @summary Update ResearchDefinition resolver.
 */
module.exports.updateResearchDefinition = function updateResearchDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeResearchDefinition
 * @static
 * @summary Remove ResearchDefinition resolver.
 */
module.exports.removeResearchDefinition = function removeResearchDefinition(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
