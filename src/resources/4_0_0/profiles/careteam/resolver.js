/**
 * @name exports.getCareTeam
 * @static
 * @summary CareTeam resolver.
 */
module.exports.getCareTeam = function getCareTeam(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getCareTeamList
 * @static
 * @summary CareTeam list resolver.
 */
module.exports.getCareTeamList = function getCareTeamList(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.getCareTeamInstance
 * @static
 * @summary CareTeam instance resolver.
 */
module.exports.getCareTeamInstance = function getCareTeamInstance(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.createCareTeam
 * @static
 * @summary Create CareTeam resolver.
 */
module.exports.createCareTeam = function createCareTeam(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.updateCareTeam
 * @static
 * @summary Update CareTeam resolver.
 */
module.exports.updateCareTeam = function updateCareTeam(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};

/**
 * @name exports.removeCareTeam
 * @static
 * @summary Remove CareTeam resolver.
 */
module.exports.removeCareTeam = function removeCareTeam(
	root,
	args,
	context = {},
	info,
) {
	let { server, version, req, res } = context;
	return {};
};
