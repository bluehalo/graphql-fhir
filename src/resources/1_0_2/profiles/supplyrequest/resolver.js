const errorUtils = require('../../../../utils/error.utils');

/**
 * @name exports.supplyrequestResolver
 * @static
 * @summary SupplyRequest Resolver.
 */
module.exports.supplyrequestResolver = function supplyrequestResolver (root, args, context, info) {
	let { server, req, res, version } = context;
	return {};
};

/**
 * @name exports.supplyrequestListResolver
 * @static
 * @summary SupplyRequest List Resolver.
 */
module.exports.supplyrequestListResolver = function supplyrequestListResolver (root, args, context, info) {
	let { server, req, res, version } = context;
	return {};
};

/**
 * @name exports.supplyrequestInstanceResolver
 * @static
 * @summary SupplyRequest Instance Resolver.
 */
module.exports.supplyrequestInstanceResolver = function supplyrequestInstanceResolver (root, args, context, info) {
	let { server, req, res, version } = context;
	return {};
};

/**
 * @name exports.supplyrequestCreateResolver
 * @static
 * @summary SupplyRequest Create Resolver.
 */
module.exports.supplyrequestCreateResolver = function supplyrequestCreateResolver (root, args, context, info) {
	let { server, req, res, version } = context;
	return {};
};

/**
 * @name exports.supplyrequestUpdateResolver
 * @static
 * @summary SupplyRequest Update Resolver.
 */
module.exports.supplyrequestUpdateResolver = function supplyrequestUpdateResolver (root, args, context, info) {
	let { server, req, res, version } = context;
	return {};
};

/**
 * @name exports.supplyrequestDeleteResolver
 * @static
 * @summary SupplyRequest Delete Resolver.
 */
module.exports.supplyrequestDeleteResolver = function supplyrequestDeleteResolver (root, args, context, info) {
	let { server, req, res, version } = context;
	return {};
};
